<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.xm06.pms.dao.NotReadInformMapper" >
  <resultMap id="BaseResultMap" type="org.xm06.pms.vo.NotReadInform" >
    <result column="user_id" property="userId" jdbcType="INTEGER" />
    <result column="group_id" property="groupId" jdbcType="INTEGER" />
    <result column="inform_id" property="informId" jdbcType="BIGINT" />
  </resultMap>
  <insert id="insertObject" parameterType="org.xm06.pms.vo.NotReadInform" >
    insert into xm06_t_not_read_inform (user_id, group_id, inform_id
      )
    values (#{userId,jdbcType=INTEGER}, #{groupId,jdbcType=INTEGER}, #{informId,jdbcType=BIGINT}
      )
  </insert>
  <insert id="insertSelective" parameterType="org.xm06.pms.vo.NotReadInform" >
    insert into xm06_t_not_read_inform
    <trim prefix="(" suffix=")" suffixOverrides="," >
      <if test="userId != null" >
        user_id,
      </if>
      <if test="groupId != null" >
        group_id,
      </if>
      <if test="informId != null" >
        inform_id,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides="," >
      <if test="userId != null" >
        #{userId,jdbcType=INTEGER},
      </if>
      <if test="groupId != null" >
        #{groupId,jdbcType=INTEGER},
      </if>
      <if test="informId != null" >
        #{informId,jdbcType=BIGINT},
      </if>
    </trim>
  </insert>

  <insert id="insert">
    INSERT INTO
	    xm06_t_not_read_inform(user_id,group_id,inform_id)
	SELECT
		#{userId} AS user_id,#{groupId} AS group_id,#{informId} AS inform_id
	FROM
		xm06_t_not_read_inform
	WHERE  ((SELECT COUNT(*)
             FROM xm06_t_not_read_inform
             WHERE user_id = #{userId}
             AND group_id = #{groupId}
             AND inform_id = #{informId}) = 0)
	LIMIT 1
   </insert>

  <delete id="removeNotReadOfGroup" parameterType="java.lang.Integer">
    delete from xm06_t_not_read_inform where group_id = #{groupId}
  </delete>
</mapper>